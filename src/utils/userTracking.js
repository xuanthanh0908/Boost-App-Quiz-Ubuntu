const UserTracking = (accessTime, userId) => {
  this.totalUser = 0
  this.CurrentTime = accessTime
  this.updateStatisticalData = () => {
    // switch(this.CurrentTime){
    //     case
    // }
  }
}
